apiVersion: 1
policies:
    - orgId: 1
      receiver: Discord
      group_by:
        - grafana_folder
        - alertname
      group_wait: 30s
      group_interval: 5m
      # only repeat alerts every 1 week if not solved in the meantime
      repeat_interval: 1w
